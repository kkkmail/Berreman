/// Placeholder for JSON-canonical IO, JSON-Schema validation, and `.binz`
/// sidecar IO (§A.7). Slice 003 fills this in. System.Text.Json arrives via
/// FSharp.SystemTextJson / the net10 shared framework (no redundant pin).
module OpticalConstructor.Storage.Storage
