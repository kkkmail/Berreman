/// Placeholder for the Avalonia + FuncUI MVU UI (§A.6). Later UI slices add the
/// model record, the pure `update`, and `view` bodies. Per P3 the model/`update`
/// MUST contain no Avalonia type. The audit-gated FuncUI clone stays
/// unreferenced until the §A.9 audit passes (AC-A8 NOT-YET-RUN).
module OpticalConstructor.Ui.Ui
