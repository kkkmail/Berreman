Made the feature list an authoritative source: removed the retrospective "Decisions settled / how we got here" section and the operator-settled/accepted/proposed/annotated discussion artifacts throughout (intro, sequencing, ribbon set, detector/analyzer, group storage, reset), leaving a forward-looking "Details left to the spec-author" only; reorganised the interaction model (S10) by command so mouse and keyboard bindings that do the same thing are combined on one line (open dialog, reset rotation, delete, duplicate, local help, slide-along-ray, reset view, zoom); made the drawer take the element kind AND the element value id (absent / None in F#) so per-device/material looks can be added later without rewiring, tied to the S9 element value id slot; stated PP is the centre of the element's rectangular bounding box; updated glossary (Drawer, PP, new Element value id entry).
