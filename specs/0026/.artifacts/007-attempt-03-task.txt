# Arc-runner worker task -- 007.slice-md

## Concrete paths for this round

The supervisor pre-resolves every per-slice path you need this
round and lists them below. Do not compute or guess these
paths: the canonical layout block describes the shape, this
section pins the concrete values for the slice you are about
to implement.

- **spec_folder:** `C:\GitHub\Berreman\specs\0026`
- **slice_md_path:** `C:\GitHub\Berreman\specs\0026\.slices\007.slice-md`
- **artifacts_folder:** `C:\GitHub\Berreman\specs\0026\.artifacts`
- **impl_log_path:** `C:\GitHub\Berreman\specs\0026\.slices\007-impl-log.md`
- **impl_plan_path:** `C:\GitHub\Berreman\specs\0026\.slices\007-impl-plan.md`
- **state_of_world_path:** `C:\GitHub\Berreman\specs\0026\.slices\007-state-of-the-world.md`
- **spec_id:** `0026`
- **slice_id:** `007`


## System prompt

Read the cross-repo system prompt in full before anything else:

- C:\GitHub\AI-Strategy-Generator\src\ai_strategy_generator\multistep\arc-runner.system-md

## Project prompt

Read the per-project prompt in full:

- C:\GitHub\Berreman\.user-md\arc-runner.user-md

## Slice spec

Implement this slice end-to-end:

- C:\GitHub\Berreman\specs\0026\.slices\007.slice-md

## Outputs you MUST write (and nowhere else)

**Save your state-of-the-world markdown to**
`C:\GitHub\Berreman\specs\0026\.slices\007-state-of-the-world.md`. **Do not write the SoW anywhere else.**

**Save your impl-log markdown to**
`C:\GitHub\Berreman\specs\0026\.slices\007-impl-log.md`. **Do not write the impl-log anywhere else.**

## Artifacts folder

Use `C:\GitHub\Berreman\specs\0026\.artifacts` for any persistent run artifacts
(captured logs, screenshots, traces, dumps). The folder is
pre-created; this path is the arc-runner's authoritative
value and supersedes any path the project prompt computes
from convention.

## Retry hint

In ConstructorView.fs, route editActive (line 204) through the existing commitIfChanged instead of the unconditional commit, so a locked-axis rotation (R3 is locked by default) and a both-off toggleEmission no longer push an empty undo step -- this one-line change closes the slice's own High 'push on a no-op edit' risk and the AC-K1 defect with no redesign. In GroupsLibrary.fs, make validate genuinely reuse the schema seam the spec names by line (R-4/G.3.1): use SchemaValidation's nested message collection (collectMessages, which recurses results.Details; expose it -- the spec already directs reusing SchemaValidation.fs:61) instead of the top-level-only results.Errors, so nested groups-file errors keep their detail. If you prefer not to touch SchemaValidation, at minimum recurse results.Details inside GroupsLibrary.validate so the nested-error detail is no longer dropped. In your state-of-the-world, stop calling that block 'reuse' until it does, record in Gotchas that 'table resize' (K.1.1) has no UI surface to wire in this slice, and reframe the redundant in-beam-state note in Deferred as a real duplicate-placement hazard rather than benign best-effort durability. Do not redesign the in-beam single-source-of-truth or the value-equality remove this cycle, and keep every gate green -- the rest of the diff is sound and this is the final slice of the arc.

