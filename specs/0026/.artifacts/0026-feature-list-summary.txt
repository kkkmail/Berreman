Folded operator comments into the Optical Constructor feature list: corrected that R1 is not a polarizer's azimuth (R1 axis is the face normal; azimuth lies in the face; at rest pose R2 gives the azimuth); made element editing menu-centric (all angles/PP edited from one local element menu, not inline one-by-one); expanded placement/snapping so changing R2/R3 re-aims R/T and everything snapped downstream travels with the rays preserving distances; added default LS (left-middle) and detector (right-middle) positions 2 m apart along the CR, moving LS drags the chain, LS has a position reset; added orphaned-element rule (only switching the tracked ray or removing one of two in-use rays orphans elements; re-snap if the ray returns within a configured distance); added that R2 on a non-horizontal ray is measured from the ray direction so table-frame angle is r2 + R2; redefined drag gestures (Shift+drag slides along the ray bounded by neighbours, Ctrl+drag moves between rays, both redraw on drop) and added ribbon drag-and-drop placement snapping to the nearest CR middle; refined element groups (preserve each member's full configuration for lossless LP/CP swapping; mutually-exclusive vs multi-select members); and noted a centralized command model (F# command DUs with a KB/mouse/both binding). Updated settled-decisions list and glossary.
