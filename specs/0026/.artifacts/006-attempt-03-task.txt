# Arc-runner worker task -- 006.slice-md

## Concrete paths for this round

The supervisor pre-resolves every per-slice path you need this
round and lists them below. Do not compute or guess these
paths: the canonical layout block describes the shape, this
section pins the concrete values for the slice you are about
to implement.

- **spec_folder:** `C:\GitHub\Berreman\specs\0026`
- **slice_md_path:** `C:\GitHub\Berreman\specs\0026\.slices\006.slice-md`
- **artifacts_folder:** `C:\GitHub\Berreman\specs\0026\.artifacts`
- **impl_log_path:** `C:\GitHub\Berreman\specs\0026\.slices\006-impl-log.md`
- **impl_plan_path:** `C:\GitHub\Berreman\specs\0026\.slices\006-impl-plan.md`
- **state_of_world_path:** `C:\GitHub\Berreman\specs\0026\.slices\006-state-of-the-world.md`
- **spec_id:** `0026`
- **slice_id:** `006`


## System prompt

Read the cross-repo system prompt in full before anything else:

- C:\GitHub\AI-Strategy-Generator\src\ai_strategy_generator\multistep\arc-runner.system-md

## Project prompt

Read the per-project prompt in full:

- C:\GitHub\Berreman\.user-md\arc-runner.user-md

## Slice spec

Implement this slice end-to-end:

- C:\GitHub\Berreman\specs\0026\.slices\006.slice-md

## Outputs you MUST write (and nowhere else)

**Save your state-of-the-world markdown to**
`C:\GitHub\Berreman\specs\0026\.slices\006-state-of-the-world.md`. **Do not write the SoW anywhere else.**

**Save your impl-log markdown to**
`C:\GitHub\Berreman\specs\0026\.slices\006-impl-log.md`. **Do not write the impl-log anywhere else.**

## Artifacts folder

Use `C:\GitHub\Berreman\specs\0026\.artifacts` for any persistent run artifacts
(captured logs, screenshots, traces, dumps). The folder is
pre-created; this path is the arc-runner's authoritative
value and supersedes any path the project prompt computes
from convention.

## Retry hint

In ConstructorView.fs, make isParameterlessInvokable return false for the four element-edit commands OpenElementDialog, ElementContextMenu, ResetRotation, and DeleteElement -- today the | _ -> true wildcard (:449) renders them as ENABLED ribbon/menu buttons that flip a model flag with no rendered front-door surface, and ResetRotation/DeleteElement additionally arm a pending the user can neither see nor clear. Do this by introducing ONE source-of-truth list of 'commands with no visible front-door effect' holding both the nine gesture-only commands and these four, and derive isParameterlessInvokable (and the RibbonTests disabled-set list) from it, collapsing today's three-way duplication; note this list is deliberately broader than applyCommand's inert-return arms, since these four DO mutate the model, so the seam is a shared list, not model-equality. The ribbon's commandButton (Ribbon.fs:224) already keys off isParameterlessInvokable, so no Ribbon.fs edit is needed -- change only ConstructorView.fs and RibbonTests.fs. Extend a ui-test to assert those four render disabled, keep tabOf/tabCommands untouched so AC-D1/AC-D2 hold and every gate stays green, and reconcile the contradictory CRLF-vs-LF Gotcha note in the SoW.

