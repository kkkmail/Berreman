# Verifier critique -- round 01 iter 01

**Verdict:** ACCEPTABLE

**Inputs verified (read in full):**
- Big spec: `C:\GitHub\Berreman\specs\0026\.spec-md`
- Guidelines: (none -- operator passed no `--splitter-guideline` files)
- Manifest: `C:\GitHub\Berreman\specs\0026\.manifest`
- Slices (in manifest order):
  - `.slices/001.slice-md` (Part A -- geometry/placement)
  - `.slices/002.slice-md` (Part B -- ray model)
  - `.slices/003.slice-md` (Part I -- localization)
  - `.slices/004.slice-md` (Part C + Part J -- table/drawer/controls)
  - `.slices/005.slice-md` (Part E + Part C interactions -- commands/MVU page)
  - `.slices/006.slice-md` (Part D + Part F -- ribbon shell/catalogue)
  - `.slices/007.slice-md` (Part G + H + K -- groups/collections/undo)

Also consulted for verification: `.spec-bundle`, `.specrc`, `.locked`,
the `.gates/` definitions, and the on-disk `OpticalConstructor.*` projects.

**Severity counts:** BLOCKER: 0, MAJOR: 1, MINOR: 3

---

## a. Coverage of big spec requirements

Every spec Part maps to exactly one owning slice, and every acceptance
criterion is owned exactly once with no duplication:

- Part A → 001 (AC-A1..A7); Part B → 002 (AC-B1..B6);
  Part C → 004 (AC-C1/C4/C5/C6) **+** 005 (AC-C2/C3); Part D → 006
  (AC-D1..D4); Part E → 005 (AC-E1..E5); Part F → 006 (AC-F1/F2);
  Part G → 007 (AC-G1..G3); Part H → 007 (AC-H1); Part I → 003 (AC-I1..I4);
  Part J → 004 (AC-J1); Part K → 007 (AC-K1/K2).
- Constraint 0.4 (single command source) is split sensibly: the registry in
  005, the ribbon/menu projection in 006. UX commitments 1-6 are each placed
  on the slice that owns the surface they describe.

No uncovered requirement and no overlapping AC ownership found.

- **MINOR** -- AC-I1 reads "**All** UI strings resolve through `strings.json`
  at run time." Slice 003 scopes this to the new constructor surface plus the
  localization *mechanism* (its `LocalizationTests` asserts runtime load /
  fallback / no-translate, not that every pre-existing legacy view string is
  migrated). That is a defensible reading -- the spec is about the new
  constructor -- but the AC's literal "all" is not pinned to a slice.
  Worth confirming the worker is not expected to retrofit the legacy Shell /
  FitView / MaterialsView strings.

## b. Independence and inter-slice order

Ordering is sound and the declared dependencies match the actual data flow:
001 (Placement) and 002 (RayModel) land first as the CRITICAL domain layer;
004 depends on 001+002; 005 on 002/003/004; 006 on 003/004/005; 007 on
001/004/005/006. 003 (localization) is deliberately sequenced ahead of the
big UI slices so later UI text resolves through `Localization.fs`.

Every file edited by more than one slice carries an explicit ownership split,
and the splits are ordered correctly:

- `Project.fs` + `optical-constructor-project.schema.json`: 001 (placement)
  before 004 (table).
- `UserEnvironment.fs` + `optical-constructor-environment.schema.json`: 003
  (`language`) before 005 (key map).
- `Program.fs`: 003 (startup localization check) before 006 (default-landing).
- `Commands.fs` created in 005, extended in 007; `ConstructorView.fs` created
  in 005, extended in 006 and 007; `Ribbon.fs` created in 006, extended in 007.

No new file is created twice; no slice references state a later slice would
produce. _No BLOCKER/MAJOR findings._

## c. Granularity

Seven slices fits the project's "3-8 per big spec" guidance. Per-slice body
size is dominated by the verbatim 0.1-0.7 constraint block (~75 lines each);
net substantive content is comparable across slices.

- **MINOR** -- Slice 007 is the heaviest and least cohesive: it bundles Part G
  (groups/detectors/separate storage) **and** Part H (collections) **and**
  Part K (cross-cutting undo/redo participation across every prior action).
  K genuinely must come last (it retrofits a snapshot `push` onto every
  project-mutating edit introduced in 001-006), so the coupling is partly
  unavoidable, and the slice flags this as High risk. Still, the operator may
  wish to split Part K into its own trailing slice so a K regression does not
  fail the Part G/H gate set with it. Slice 005 is also large (registry + full
  `ConstructorView` MVU page + Part C interactions) but its pieces are tightly
  related (the view consumes the registry), so it does not warrant a split.

## d. Naming convention

Slice files (`.slices/001.slice-md` ... `007.slice-md`) and the manifest
(`.manifest`) follow the canonical dot-file layout in the system prompts'
"Spec folder layout" section (3-digit zero-padded `<NNN>.slice-md`, fixed
`.manifest` name). The manifest body slice list matches the files on disk
exactly. _No findings_ (the rubric's legacy `<nnn>-<mm>-<kk>-<desc>.md` /
`<arc-root>-slices.manifest` pattern is superseded by that layout section and
does not apply here).

## e. Touch-set realism

Spot-checked every edited path and the key reused anchors against the repo:

- All edited files exist: `BeamTree.fs`, `Project.fs`, `Units.fs`,
  `Shell.fs`, `AppShell.fs`, `Schematic.fs`, `ConstructionView.fs`,
  `UserEnvironment.fs`, `Program.fs`, `ProjectFile.fs`, `SchemaValidation.fs`,
  `History.fs`, both edited schema JSONs, and the `ProjectJsonRoundtripTests`
  / `EnvironmentRoundTripTests` harnesses the slices reuse.
- All new files are correctly absent today (Placement, RayModel, Table,
  Groups, Drawer, ConstructorTable, ConstructorView, Commands, Ribbon,
  Controls, Localization, LocalHelp, strings.json, GroupsLibrary, the new
  groups schema, and the new test files).
- Anchor accuracy is high: `ConstructorElement` (BeamTree.fs:35) has exactly
  `Source | Polarizer | Sample | Lens | CurvedMirror | FlatMirror | Analyzer |
  Detector`, so the A.5.2 `toConstructorElement` mapping (LS→Source, LP/CP→
  Polarizer, Detector→Detector, Analyzer never surfaced) is realisable as
  written. `EnvironmentSettings` is at UserEnvironment.fs:141, `FavoriteGroup`
  at :126, `OpticalConstructorProject` at Project.fs:26, and `EditHistory`/
  `push`/`undo`/`redo` at History.fs:20/33/39/47 -- all matching the cited
  lines.

Note: the project `arc-runner.user-md` still describes Berreman as a "pure F#
numerical solver ... no UI"; the repo in fact already contains the full
`OpticalConstructor.*` Avalonia/FuncUI stack the spec extends, so the slices'
touch-set is real despite that stale prose. _No findings._

## f. Risk calibration

Tags are reasonable. Slices that mutate shared infrastructure are tagged
appropriately: 001/004 (project schema + aggregate round-trip) Medium;
003 (app entry point) Medium; 005 (largest UI slice) High; 006 (reroutes the
app's default landing through `RootModel`/`RootMsg`) High; 007 (cross-cutting
undo/redo) High. No "low-risk" slice touches production-critical code and no
high tag is attached to a markdown-only change. _No findings._

## g. Manifest header

- Required keys all present and valid: `repo_root` (`C:/GitHub/Berreman`,
  resolves), `branch` (`0026`), `worker_timeout_sec` (14400 -- within
  600-43200), `worker_hangup_idle_sec` (3600 -- ≥300 and < worker_timeout),
  `worker_review_max_cycles` (3), `max_total_route_backs` (30).
  `data_version: 5` matches `.specrc.data_version: 5`.
- Project-prompt resolution: `.spec-bundle.user_files` /
  `.specrc.user_files` map splitter→`.user-md/splitter.user-md` and
  arc-runner→`.user-md/arc-runner.user-md`; both resolve on disk.
- Gate id resolution: every id in `.spec-bundle.gates`
  (`build`, `unit-tests`, `constructor-unit-tests`, `ui-smoke`, `ui-tests`,
  `impl-log-structure`, `state-of-world-structure`) resolves to a definition
  under `.gates/` (root, `.gates/Berreman/`, `.gates/OpticalConstructor/`).
  Each slice's per-slice roster in the manifest `slice_gates:` block is a
  subset of those, and all resolve.
- Artifacts folder `specs/0026/.artifacts/` exists.
- Slice list in the manifest body matches the on-disk slice files exactly.
- No slice spec routes persistent output to `%TEMP%` / `%LOCALAPPDATA%` or
  outside the per-arc artifacts folder.

- **MAJOR** -- Gate-roster **delivery mechanism is contract-ambiguous, and the
  splitter authored only one of the two forms.** Both system prompts state the
  per-slice roster "lives in its sibling `<slice-stem>.gates` snapshot file
  (authored by the splitter from the spec's `.locked` gates)" and that "the
  manifest header carries no `gates_file:` key anymore"
  (`verifier.system-md` Phase 1.3; `arc-runner.system-md` step 5). No
  `.slices/<NNN>.gates` snapshot files exist -- the `.slices/` folder holds
  only `001.slice-md`..`007.slice-md`. Instead the roster lives in a
  `slice_gates:` YAML block in the manifest body, which is exactly what the
  project `arc-runner.user-md` mandates ("The arc-runner reads each slice's
  gate set from the manifest's `slice_gates:` block"). So the two contracts
  contradict each other. The manifest form is internally well-formed and
  matches the project doc, so this is likely intentional project divergence
  rather than a defect -- but if the running arc-runner follows its system
  prompt it will look for the absent snapshot files and find no roster.
  Recommend the reconciler/operator confirm which mechanism the current tool
  uses; if it is the snapshot-file form, the splitter must additionally emit
  `.slices/<NNN>.gates` from the `.locked` gates.

## h. Per-slice quality

Every slice carries Scope, numbered Requirements (R-N traceable to the spec's
A/B/.../K numbering and the owned ACs), Non-requirements, Files modified
(planned), Testing plan (with the applicable gate set called out), Risks, and
Hand-off. No required section is missing in any slice. _No findings._

## i. Other

- Informational (not a splitter-output defect): the `.user-md/*.user-md`
  files internally cite their own location as `./.user-md/Berreman/
  arc-runner.user-md`, but the canonical `user_files` map and the task file
  resolve them at `.user-md/arc-runner.user-md` (no `Berreman/` segment). That
  inconsistency lives in the project prompt files, not in the manifest or
  slices, so it is outside this critique's scope -- flagged only so it is not
  mistaken for a slice problem downstream.
- No input was skimmed; all listed inputs were read in full.

---

## Recommendations (priority order)

1. **(MAJOR, §g)** Resolve the gate-roster mechanism mismatch. Confirm whether
   the active arc-runner reads the manifest `slice_gates:` block (per the
   project `arc-runner.user-md`) or sibling `.slices/<NNN>.gates` snapshot
   files (per both system prompts). If the latter, have the splitter emit the
   snapshot files from `.locked`; if the former, no change is needed but the
   divergence from the cross-repo protocol should be recorded.
2. **(MINOR, §c)** Consider whether Part K (undo/redo participation) should be
   its own trailing slice rather than bundled with Part G/H in 007, so a
   cross-cutting K regression does not fail the groups/collections gate set.
3. **(MINOR, §a)** Confirm the intended reach of AC-I1 ("all UI strings") --
   new constructor surface only vs. retrofitting existing legacy views -- and
   pin it explicitly on slice 003 if legacy migration is in scope.
