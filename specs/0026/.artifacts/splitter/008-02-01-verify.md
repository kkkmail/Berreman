# Verifier critique -- round 02 iter 01

**Verdict:** ACCEPTABLE

**Inputs verified (read in full):**
- Big spec: `C:\GitHub\Berreman\specs\0026\.spec-md`
- Guidelines: (none -- operator passed no `--splitter-guideline` files)
- Manifest: `C:\GitHub\Berreman\specs\0026\.manifest`
- Slices (in manifest order):
  - `.slices/001.slice-md` (Part A -- placement/geometry domain)
  - `.slices/002.slice-md` (Part B -- ray model / snapping / Snell)
  - `.slices/003.slice-md` (Part I -- EN/RU localization)
  - `.slices/004.slice-md` (Part C + Part J -- table/drawer/render geometry + controls)
  - `.slices/005.slice-md` (Part E + Part C interactions -- commands + MVU surface)
  - `.slices/006.slice-md` (Part D + Part F -- ribbon shell + catalogue/value-id modal)
  - `.slices/007.slice-md` (Part G + H + K -- groups/collections/detectors + undo participation)
- Also cross-checked: `.spec-bundle`, `.spec-gates`, the seven `.slices/00N.gates`
  snapshots, the resolved gate definitions under `.gates/`, and the referenced
  source anchors in the live `OpticalConstructor` tree.

**Severity counts:** BLOCKER: 0, MAJOR: 0, MINOR: 3

---

## a. Coverage of big spec requirements

_no findings_

Every spec part and acceptance criterion maps to exactly one owning slice, and
every shared concern that two slices touch is partitioned with an explicit
ownership note (no double-coverage):

- Part A / AC-A1..A7 -> 001.
- Part B / AC-B1..B6 -> 002.
- Part I / AC-I1..I4 -> 003.
- Part C: geometry AC-C1, C4, C5, C6 -> 004; interaction AC-C2, C3 -> 005;
  Part J / AC-J1 -> 004.
- Part E / AC-E1..E5 -> 005.
- Part D / AC-D1..D4, Part F / AC-F1..F2 -> 006.
- Part G / AC-G1..G3, Part H / AC-H1, Part K / AC-K1..K2 -> 007.

All 41 acceptance criteria are assigned; no requirement is left uncovered and no
two slices claim the same AC. Binding constraints 0.1-0.7 and the six UX
commitments are inherited verbatim into every slice and are pinned to concrete
slices where they impose work (UX 1 -> 006, UX 2/3/4 -> 005, UX 5/6 -> 007).

## b. Independence and inter-slice order

_no findings_

The order is a clean dependency topology and each slice's declared `Depends on`
line matches it:

- 001 (pure domain, no deps) -> 002 (dep 001) -> 003 (UI infra, independent of
  001/002) -> 004 (dep 001/002) -> 005 (dep 002/003/004) -> 006 (dep 003/004/005)
  -> 007 (dep 001/004/005/006).

Domain/core lands before the UI surface, satisfying the `splitter.user-md`
sequencing rule. The six multiply-edited files are each split by owner with a
documented hand-off so no later slice silently overwrites an earlier one:
`Project.fs` (placement=001 / table=004 / optional group ref=007),
`optical-constructor-project.schema.json` (placement=001 / table=004),
`UserEnvironment.fs` and the environment schema (language=003 / key map=005),
`Program.fs` (localization check=003 / default-landing=006),
`Commands.fs` and `ConstructorView.fs` (created 005, extended 006/007). The
placement/table schema anchors are explicitly called out as "keep stable" for
the follow-on edits.

## c. Granularity

- **MINOR** -- Slice 005 is materially heavier than its neighbours: it delivers
  the whole `Commands.fs` registry (E.1-E.8), the full `ConstructorView.fs` MVU
  page, every mouse/keyboard gesture, the configurable-key-map persistence, and
  the Part C view interactions (pan/zoom/rotate/reset/selection) in one round.
  It is self-tagged "High". The responsibilities are cohesive (one interaction
  surface plus the page that hosts it), and AC-E1 genuinely wants the registry
  proven by mounting `ConstructorView`, so this is not "unrelated
  responsibilities" -- but if any slice in the arc is a candidate to overrun a
  session it is this one. Optional: peel `Commands.fs` + `CommandRegistryTests.fs`
  (the pure registry and its single-source proof, AC-E1) into a slice ahead of
  `ConstructorView.fs`; the registry is testable headless without the page.

- **MINOR** -- Slice 007 bundles three spec parts (G groups/detectors/storage,
  H collections, K undo/redo participation). G/H are one storage-shaped cluster;
  K is a cross-cutting pass that wires a snapshot `push` into every
  project-mutating edit introduced in 001-006. K legitimately must be last, so
  co-locating it with the final domain features is defensible, but the worker
  carries both a new-feature surface and an across-the-codebase audit in a
  single round. Acceptable as drafted; flagged so the reconciler is aware the
  final slice is broad.

## d. Naming convention

_no findings_

The arc uses the canonical dot-prefixed layout from the system prompt's "Spec
folder layout" (`.slices/00N.slice-md`, `.manifest`), not the older
`<nnn>-<mm>-<kk>-<description>.md` / `<arc-root>-slices.manifest` shape in rubric
(d). The manifest body lists exactly the seven `.slices/00N.slice-md` files that
exist on disk, so arc-runner slice-discovery is unaffected. No deviation that
matters.

## e. Touch-set realism

_no findings_

Spot-checked every load-bearing reference against the live tree; all resolve:

- `BeamTree.fs` -- `ConstructorElement` (:35) has exactly the cases the A.5.2
  mapping needs (`Source`/`Polarizer`/`Sample`/`Lens`/`CurvedMirror`/`FlatMirror`/
  `Analyzer`/`Detector`); `BeamBranch` Reflected/Transmitted (:21), `BeamNode`
  (:53), `defaultUnit` (:59), `attach` mirror rule (:74), `solve` (:91),
  `branchEmField` (:98), `routeAndSolve` (:117) all match the cited lines.
- `Shell.fs` -- `Page` (:73), `RootModel` (:94), `RootMsg` (:121),
  `navButton` (:500), `navBar` (:510), `constructionBody` (:572) all present.
- `ConstructionView.fs` -- `positiveCta` (:40) / `negativeCta` (:41) present.
- `optical-constructor-project.schema.json` -- the `constructorElement` `$def`
  is permissive (`true`) at :201, exactly as 001/004 assume; reserved anchors
  intact.
- Environment schema, `UserEnvironment.fs`, `Program.fs`, the `OpticalConstructor.Ui`
  / `.App` / `.Tests` / `.Ui.Tests` projects, and the `.fsproj` content-item
  pattern 003 mirrors all exist.

New-file placements (`Placement.fs`, `RayModel.fs`, `Table.fs`, `Groups.fs` under
`.Domain`; `Drawer.fs`, `ConstructorTable.fs`, `Controls.fs`, `Commands.fs`,
`ConstructorView.fs`, `Ribbon.fs`, `Localization.fs`, `LocalHelp.fs`,
`strings.json` under `.Ui`; `GroupsLibrary.fs` + groups schema under `.Storage`;
test files under `.Tests` / `.Ui.Tests`) are consistent with the existing project
structure.

## f. Risk calibration

_no findings_

Risk tags track the blast radius. The two slices that mutate shared
infrastructure -- 006 (reroutes the app front door through `RootModel`/`RootMsg`,
risking every existing panel test) and 007 (cross-cutting undo wiring + the
separate app-data store) -- are tagged High. The schema/serialization-touching
domain slices (001, 004) and the settings/entry-point slice (003) are Medium.
Nothing is mis-tagged low while touching the MVU shell, schema, or settings
spine.

## g. Manifest header

_no findings_

- Required keys all present and sane: `repo_root` (`C:/GitHub/Berreman`),
  `branch` (`0026`), `worker_timeout_sec` 14400 (4 h; within [600, 43200]),
  `worker_hangup_idle_sec` 3600 (1 h; >= 300 and < timeout),
  `worker_review_max_cycles` 3, `max_total_route_backs` 30. `data_version` 5
  present.
- Derived paths resolve: the role prompt from `.spec-bundle.user_files["arc-runner"]`
  (`.user-md/arc-runner.user-md`) exists; the per-arc `.artifacts/` folder exists.
- Every gate id in `.spec-bundle.gates` resolves to a definition: `build`,
  `unit-tests` (`.gates/Berreman/`), `constructor-unit-tests`, `ui-smoke`,
  `ui-tests` (`.gates/OpticalConstructor/`), `impl-log-structure`,
  `state-of-world-structure` (`.gates/`).
- The manifest's `slice_gates:` block is internally consistent with each slice's
  declared Testing-plan gate list -- including the deliberate omission of
  `constructor-unit-tests` from slice 006 (UI/App-only, no Domain/Storage glob
  hit) and from slices 001/002 the omission of `ui-smoke`/`ui-tests` (domain-only).
- All seven `.slices/00N.gates` snapshot files are present (001-007); the
  manifest body slice list matches the seven slice files on disk.
- No slice routes a persistent run/log artifact outside the per-arc
  `.artifacts/` folder. (Slice 007's groups/collections JSON at the per-user
  `Softellect/Berreman/OpticalConstructor` app-data path is *application operator
  state*, not a worker-round capture, and the spec justifies the location under
  UX 6 -- the rubric's artifact-placement rule does not apply to it.)

## h. Per-slice quality

_no findings_

All seven slices carry every required section -- Scope, numbered Requirements
(R-N mapped back to the spec's A.x/B.x/... clauses), Non-requirements, Files
modified (planned), Testing plan, Hand-off -- plus Risks, the inherited binding
constraints, owned ACs, and owned file:line references. No slice is missing a
required section.

## i. Other

- **MINOR** -- The project prompt `arc-runner.user-md` ("Project shape") still
  describes Berreman as "pure F# numerical code -- no UI, no Android, no A/V
  harness," yet the arc, the spec, and the live tree are a full Avalonia + FuncUI
  desktop app under `Berreman/OpticalConstructor/` with `ui-smoke`/`ui-tests`
  gates. The slices correctly target the real UI tree, so this does not break the
  slicing, but a worker who reads that section literally could be misled about
  whether UI work is in scope. This is a project-prompt staleness issue (outside
  the splitter's output), surfaced here only because it materially contradicts
  the gate set the same file later defines.

(Read confirmation: every input listed in the task file was read in full; no
input was skimmed or skipped.)

---

## Recommendations (priority order)

1. None blocking. The slicing is internally consistent, fully covers the spec,
   orders dependencies correctly, and every anchor / gate / path resolves on
   disk -- accept and proceed.
2. (Optional, MINOR) If session-budget risk is a concern, consider splitting
   slice 005 so the pure `Commands.fs` registry + `CommandRegistryTests.fs`
   (AC-E1 single-source proof) land ahead of the `ConstructorView.fs` MVU page.
3. (Awareness, MINOR) Treat slice 007 as the broad final slice it is (G+H+K with
   a cross-cutting undo-participation audit); no reslice needed, but expect it to
   be the heaviest review.
4. (Housekeeping, MINOR) Refresh the `arc-runner.user-md` "Project shape" section
   so its "no UI" description no longer contradicts the FuncUI app and the
   `ui-smoke`/`ui-tests` gates.
