# .manual

This subfolder is for user-authored files: task statements, notes,
attached screenshots, reference documents. The spec-writer does not
read or rewrite anything inside this folder.

Anything you want the spec-writer to consult belongs elsewhere in the
spec folder; anything that is only for human reference belongs here.
