# Verifier critique -- round 05 iter 01

**Verdict:** ACCEPTABLE

**Inputs verified (read in full):**
- Big spec: `C:\GitHub\Berreman\specs\0022\.spec-md` (1053 lines, Parts A–J + acceptance criteria + files-in-scope + test artifacts + references)
- Guidelines: (none -- the operator passed no `--splitter-guideline` files)
- Manifest: `C:\GitHub\Berreman\specs\0022\.manifest`
- Slices (in manifest order): `001`–`016` (`.slices/001.slice-md` … `.slices/016.slice-md`)
- System prompts: `verifier.system-md`, `arc-runner.system-md`
- User prompts: `Berreman/splitter.user-md`, `Berreman/arc-runner.user-md`

**Severity counts:** BLOCKER: 0, MAJOR: 0, MINOR: 3

Disk spot-checks performed (touch-set / reuse-claim realism, §e / §g):
- `Berreman/Berreman.slnx`, `Berreman/OpticalModel/Primitives.fs` (the untouched stub), `C:\GitHub\Avalonia.FuncUI.Clone\`, `C:\GitHub\Softellect\Sys\Core.fs` + `Primitives.fs` — all present.
- `Analytics/Examples/` — every gallery `.fsx` named by §J.11 (`MultilayerThinFilm.fsx`, `MultilayerThinFilm_EUV.fsx`, `ActiveCrystal.fsx`, `Wedge_Glass.fsx`, `Wedge_BiaxialCrystal.fsx`, `Glass_Dispersive.fsx`) present.
- Engine line-refs: `FieldFunctions.fs:93` carries the commented-out `member em.muellerMatrix` (F.3 edit target — confirmed commented); `Fields.fs:411` `rotateY` adds onto `incidenceAngle` (matches AC-C3 wording); `Solvers.fs:149` is the `(emf : EmField, system : ShortOpticalSystem)` overload (B.4); `Solvers.fs:199` `OpticalSystemSolver`; `FourierTransform.fs:74` `createGaussian` — all confirmed.
- Gate resolution: `build` → `.gates/Berreman/build.gates`; `unit-tests` → `.gates/Berreman/unit-tests.gates`; `constructor-unit-tests` → `.gates/OpticalConstructor/constructor-unit-tests.gates` (`pass_when: exit_code: 0`, command targets `OpticalConstructor.Tests.fsproj`). All three manifest gate ids resolve.

---

## a. Coverage of big spec requirements

_no findings_

Every directive A.0–A.10, B.0–B.11, C.0–C.10, D.0–D.12, E.0–E.10, F.0–F.10, G.0–G.11, H.0–H.11, I.0–I.8, J.0–J.12 maps to exactly one owning slice requirement, and every acceptance criterion (AC-A1..A8, AC-B1..B11, AC-C1..C8, AC-D1..D8, AC-E1..E10, AC-F1..F9, AC-G1..G9, AC-H1..H10, AC-I1..I11, AC-J1..J12) is owned by exactly one slice:
- A → 001 (A1/A8), 002 (A3/A4/A5/A7), 003 (A2/A6)
- B → 002 (B2/B3/B4/B8/B11), 005 (B1/B5/B6/B7/B9/B10)
- C → 006 (all); D → 002 (D1/D2), 003 (D6), 004 (D3/D4/D5/D7/D8)
- E → 007 (all); F → 008 (F1/F2/F4/F6/F9 + smoke guards), 011 (F3/F5/F7/F8 full)
- G → 009 (G1/G2), 010 (G3/G4), 011 (G5..G9)
- H → 012 (all); I → 003 (I1..I5), 013 (I6..I11)
- J → 014 (J1..J5), 015 (J6..J9), 016 (J10..J12)

No uncovered requirement; no two slices claim the same AC. The seven binding constraints (§0) are reproduced verbatim in every slice and respected (no non-SI storage, no FsPickler of the project model, no clone reference, no new C# project, tier labels preserved).

## b. Independence and inter-slice order

- **MINOR** — Slice 011 (line ~121, "Files modified / Edited") attributes the `.gitignore` `*.binz`/`*.autosave` rule to "slice 012 / Part I". This is a stale cross-reference: slice 012 is Part H (charts) and edits no `.gitignore`; the `*.binz` entry is owned by **slice 003** (§I.4) and `*.autosave` by **slice 013** (§I.5). No build impact — slice 003 already lands `*.binz` before slice 011 runs, so fit-history sidecars are covered — but the pointer should read "slice 003 (`*.binz`) / slice 013 (`*.autosave`)" to avoid a worker chasing a non-existent edit in slice 012.

The build order is otherwise sound and dependencies flow forward only: 001→002→003→004→005; 006 depends only on 002; 007 on 002; 008 on the engine; 009 on engine, 010 on 008(§F.5 `psiDelta`)+009, 011 on 008/009/010; 012 on 008; 013 on 003/004/012; 014 on 003/004/012; 015 supplies `Validation.fs`; 016 on 011/015. The one genuine forward-dependency risk — slice 014's §J.2 `RepeatBuilder` "rejects count<1 through the J.9 validation seam" while `Validation.fs` is not created until slice 015 — is **handled correctly**: slice 014 explicitly forbids `RepeatBuilder.expand` from referencing `Validation.fs` (keeps it `List.replicate count cell |> List.concat`) and re-homes the `R<1` rejection + its test (AC-J9) to slice 015, so the one-round-per-slice arc-runner never sees an unresolved symbol. Good.

## c. Granularity

- **MINOR** — Slice 012 (Part H) bundles seven UI view modules (`ChartSettings`, `SeriesData`, `Plot1DView`, `Plot3DView`, `Readout`, `PolarizationPlots`, `CieView`) plus four test files in one slice. It is the widest slice by file count. It is acceptable (each file is thin because the data path is reused, not re-invented, and the modules are tightly cohesive) but is the highest-effort UI slice — the worker should budget accordingly. Not worth splitting.

Otherwise granularity is well-judged: 16 slices for a 10-part spec with ~40 new files and ~70 ACs, with the heaviest parts (A, G, J) each split (A→001/002/003; G→009/010/011; J→014/015/016). No slice is 5–10× its neighbours; each is plausibly one session.

## d. Naming convention

_no findings_

The files follow the **canonical cross-repo dot-prefixed layout** documented in `verifier.system-md` / `arc-runner.system-md` ("Spec folder layout"): slice specs at `.slices/<NNN>.slice-md` (`001`…`016`) and the manifest at `.manifest`. The rubric's generic `<nnn>-<mm>-<kk>-<description>.md` / `<arc-root>-slices.manifest` pattern is superseded by that canonical layout for this tool; the names are correct and arc-runner slice-discovery is unaffected.

## e. Touch-set realism

_no findings_

All edits to pre-existing files target real files (`Berreman.slnx`, `FieldFunctions.fs`, `Analytics/Analytics.fsproj`, `BerremanTests/BerremanTests.fsproj`, `.gitignore`); the verified line-references (F.3 commented member, `rotateY`, the `EmField` solver overload, `createGaussian`) are accurate. All net-new files land under the new `OpticalConstructor/` tree the arc creates. No slice claims a file that should already exist but does not. Persistent-artifact paths are disciplined: slice 008 explicitly writes its `.binz` smoke artefact under the supervisor-supplied per-arc artifacts folder (never `%TEMP%`/`%LOCALAPPDATA%`), and all sweep/fit sidecars route under the project working/sidecar directory.

Strength worth noting: the big spec's "Test artifacts" list names a separate `OpticalConstructor.StorageTests/` project, but the slices (003, 013) consistently redirect every test into the single gated `OpticalConstructor.Tests.fsproj` host (reconciler round-2 §g) — which is exactly the project the `constructor-unit-tests` descriptor runs. No orphaned/un-gated test project results.

## f. Risk calibration

_no findings_

Risk tags are reasonable: 002 (domain coupling core) Medium, 011 (Wolfram ground-truth absent in-repo) Medium/high, others Low/Medium. The one shared-infrastructure mutation — slice 008's edit to engine `FieldFunctions.fs:93` — is correctly de-risked as a pure delegation to the existing `MuellerMatrix.fromEmFields`, shipped with a mandatory same-slice regression guard (`MuellerMatrixTests.fs`) and gated by `unit-tests` (BerremanTests) staying green. Tagging that specific edit Low while the slice is Medium overall is defensible.

## g. Manifest header

_no findings_

- Required keys all present: `repo_root` (`C:/GitHub/Berreman`, resolves), `branch` (`0022`), `worker_timeout_sec` (14400, in [600,43200]), `worker_hangup_idle_sec` (3600, ≥300 and < timeout), `worker_review_max_cycles` (3), `max_total_route_backs` (30).
- No `gates_file:` key — correct per the current contract (per-slice `.gates` snapshots + the manifest `slice_gates:` block are the source of truth; both agree on `[build, unit-tests, constructor-unit-tests]` for all 16 slices).
- All gate ids resolve on disk (verified above); the previously-flagged `constructor-unit-tests` descriptor exists, is group-correct, and targets the slice-001-created test host.
- Per-role project prompts resolve (`splitter.user-md`, `arc-runner.user-md` both read in full).
- Per-arc artifacts folder (`specs/0022/.artifacts/`) exists.
- Slice list in the manifest body (`001`–`016`) matches the sixteen slice files on disk.

## h. Per-slice quality

_no findings_

Every slice carries Scope, numbered Requirements (R-N), Non-requirements, Files modified (planned), Testing plan, Risks, and Hand-off, plus inherited binding constraints, owned ACs, files-in-scope, and file:line references. No required section is missing. Each slice emits the mandatory state-of-the-world + `gates:` YAML baseline instruction for the next slice's `count_at_least` gate.

## i. Other

- **MINOR** — AC-F3 (the completed `em.muellerMatrix` equals `MuellerMatrix.fromEmFields`) is asserted in **both** slice 008 (`MuellerMatrixTests.fs`, the mandatory same-slice regression guard for the `FieldFunctions.fs:93` engine edit) and slice 011 (`OptimizationTests.fs`, the full Part-F equivalence suite). This is deliberate and documented (engine-edit guard vs. full suite), but the two assertions are near-identical; if the reconciler wants to trim, keep slice 008's as the minimal guard and slice 011's as canonical, and note the intended relationship so they don't drift. Not blocking.

- _Informational (not a finding):_ the big spec is internally ambiguous about where `OpticalConstructorProject` lives — §A.1 places the "project aggregate (§A.7)" in `OpticalConstructor.Domain`, while §A.7's prose says "`OpticalConstructor.Storage` MUST: Define the root aggregate". Slice 002 resolves this in favour of **Domain** (so `Storage`/`Optimization`/`Ui` can all reference the type, and the §A.4 beam tree that lives in Domain can be a field of it), with serialization owned by slice 003. This is the dependency-correct reading and the slice flags the trace explicitly; the resolution is sound.

---

## Recommendations (priority order)

1. **(MINOR, §b)** Fix slice 011's `.gitignore` ownership pointer: replace "owned by slice 012 / Part I" with "owned by slice 003 (`*.binz`) and slice 013 (`*.autosave`)". Purely a prose correction; no behaviour change.
2. **(MINOR, §i)** In slice 011, add one line clarifying that the AC-F3 Mueller-equivalence assertion is the canonical/full version and slice 008's `MuellerMatrixTests.fs` is the minimal engine-edit regression guard, so the two are not mistaken for redundant duplication and stay coordinated.
3. **(MINOR, §c)** No action required, but flag for the worker that slice 012 (Part H) is the widest slice (7 view modules + 4 test files) and the highest-effort UI round; budget the session accordingly. Splitting is not recommended given the shared `SeriesData`/`ChartSettings` spine.

The slicing has zero BLOCKERs and zero MAJORs, complete and singly-owned coverage of all directives and ACs, a sound forward-only build order (including a correctly-handled deferred validation seam), a valid manifest header with all gates resolving, and accurate engine reuse-claims. The three MINORs are cosmetic/coordination nits the reconciler can apply individually or waive. Verdict: **ACCEPTABLE**.
