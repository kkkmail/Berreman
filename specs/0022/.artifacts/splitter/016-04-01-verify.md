# Verifier critique -- round 04 iter 01

**Verdict:** NEEDS-REVISION

**Inputs verified (read in full):**
- Big spec: `C:\GitHub\Berreman\specs\0022\.spec-md` (1053 lines, Parts A–J, all ACs, files-in-scope, test artifacts)
- Guidelines: (none -- operator passed no `--splitter-guideline` files)
- Manifest: `C:\GitHub\Berreman\specs\0022\.manifest`
- Slices (manifest order): `.slices/001`–`016.slice-md`
- Also consulted for resolution checks: `.spec-bundle`, `.gates/Berreman/{.gates-md,build.gates,unit-tests.gates}`, the two system prompts, `splitter.user-md`, `arc-runner.user-md`, and spot-checked engine touch-points (`FieldFunctions.fs:93`, `OpticalModel/Primitives.fs:9`, `Berreman/OpticalConstructor/**` absence, `Analytics/Examples/*.fsx`).

**Severity counts:** BLOCKER: 1, MAJOR: 0, MINOR: 2

---

## a. Coverage of big spec requirements

_no findings_

Every directive A.1–A.10, B.1–B.11, C.1–C.10, D.1–D.12, E.1–E.10, F.1–F.10,
G.1–G.11, H.1–H.11, I.1–I.8, J.1–J.12 maps to exactly one owning slice, and
every acceptance criterion AC-A1..A8, AC-B1..B11, AC-C1..C8, AC-D1..D8,
AC-E1..E10, AC-F1..F9, AC-G1..G9, AC-H1..H10, AC-I1..I11, AC-J1..J12 is claimed
by a slice. I cross-walked the spec's entire "Files in scope" list against the
slices: every new/edited file is owned by exactly one slice with no orphan and
no duplicate (e.g. `FieldFunctions.fs` edit → 008 only; `.gitignore` split
`*.binz`→003 / `*.autosave`→013 is deliberate and non-overlapping). The
parent's separate `OpticalConstructor.StorageTests` project is intentionally
consolidated into the single `OpticalConstructor.Tests` host (reconciler
round-2 §g), applied consistently across slices 003/013 — not a coverage gap.

## b. Independence and inter-slice order

_no findings_

The dependency chain is sound and the slices are explicit about their
predecessors: 002 (domain types) precedes everything that consumes them; 003
(storage core) precedes 004 (materialEntry `$def`), 005, 013; 004
(`resolveMaterial`) precedes 005 (medium selectors, §B.6) and 014 (drag-drop,
§J.4); 008 (Part F `fieldDepthProfile`/colorimetry) precedes 012 (Part H);
009→010→011 stage Part G; 015 (`AppShell`) precedes 016 (`JobRunner` status
area). Forward-reference hazards are handled deliberately — slice 014's
`RepeatBuilder.expand` is kept a pure `List.replicate … |> List.concat` with NO
reference to slice-015's `Validation.fs`, because the one-round-per-slice
arc-runner could not compile a later-slice symbol; the `R<1` rejection and its
test (AC-J9) are correctly relocated to slice 015. The slice-008 engine edit
(`FieldFunctions.fs:93`) lands before any consumer of `em.muellerMatrix`.

## c. Granularity

- **MINOR** -- Slice 002 is materially heavier than its neighbours: it owns
  A.3, A.4, A.5, A.7 (aggregate + the nine `$defs` anchors), A.10, B.2, B.3,
  B.4, B.8, B.11, and D.1–D.4 in one round. It is cohesive (it is deliberately
  "the core immutable domain model / coupling core") and is almost entirely
  type definitions plus small pure functions + units, so it is plausibly one
  session; but it is the single largest concentration of directives in the arc
  and a wrong type shape here forces edits across every later slice (the slice
  acknowledges this in its own Risks). Worth keeping in mind during review; not
  worth re-splitting. Part-sized slices (006/Part C, 007/Part E, 008/Part F,
  012/Part H each ~10–11 directives) are otherwise consistently calibrated, and
  the large/complex Parts (G, I, J) are correctly sub-divided into 009–011,
  003+013, and 014–016.

## d. Naming convention

_no findings_

The rubric's `<nnn>-<mm>-<kk>-<description>.md` / `<arc-root>-slices.manifest`
pattern is superseded here by the canonical dot-prefixed spec-folder layout
that BOTH system prompts define (`.slices/<NNN>.slice-md`, `.manifest`). The
slices follow that layout (`001.slice-md` … `016.slice-md`) and the manifest is
`.manifest`; slice discovery is by the manifest body list, which matches the
files on disk 001–016. No deviation.

## e. Touch-set realism

_no findings_

Spot-checks confirm the load-bearing engine touch-points are real:
- `FieldFunctions.fs:93` is in fact the commented-out
  `// member em.muellerMatrix : MuellerMatrix =` — slice 008's single
  engine-core edit target is accurate.
- `OpticalModel/Primitives.fs:9` is the `let x = 1` stub the slices flag as
  leave-untouched.
- `Berreman/OpticalConstructor/**` does not yet exist — consistent with slice
  001 creating all five projects greenfield; no slice assumes a pre-existing
  constructor file.
- All `Analytics/Examples/*.fsx` cases slice 016 (§J.11) sources the gallery
  from exist (`MultilayerThinFilm.fsx`, `MultilayerThinFilm_EUV.fsx`,
  `ActiveCrystal.fsx`, `Wedge_Glass.fsx`/`Wedge_BiaxialCrystal.fsx`,
  `Glass_Dispersive.fsx`).

The remaining engine `file:symbol` citations were already claim-checked
upstream and are internally consistent across slices; I found no slice claiming
to touch a file that an earlier slice was not creating.

## f. Risk calibration

_no findings_

Infra-mutating slices are tagged appropriately: slice 001 (solution file + five
new projects + broad NuGet resolution) is "Low/medium" with empty-placeholder
isolation; slice 002 (the coupling core) is "Medium"; slice 008's one-line
engine delegation (`FieldFunctions.fs:93` → `MuellerMatrix.fromEmFields`,
verified trivial) is correctly "Low" and ships a same-slice regression guard;
slice 009 (ALGLIB GPL `PackageReference`) and 011 (Wolfram-validation
re-scoping) carry the right "Medium"/"Medium-high" weight. No "low risk on
shared infra" or "high risk on a markdown file" miscalibration.

## g. Manifest header

- **BLOCKER** -- The `constructor-unit-tests` gate has no definition file and no
  slice authors one. `.spec-bundle.gates` =
  `["build","unit-tests","constructor-unit-tests"]`, and every slice's gate
  roster (manifest `slice_gates:` and the `.slices/NNN.gates` snapshots) is
  `[build, unit-tests, constructor-unit-tests]` — but the project gates folder
  `.gates/Berreman/` contains only `build.gates` and `unit-tests.gates`. There
  is no `.gates/Berreman/constructor-unit-tests.gates` descriptor (the file that
  supplies the gate's `command`/`cwd`/`pass_when`, as `unit-tests.gates` does).
  Per `arc-runner.user-md` ("If a slice introduces one, propose a new
  `.gates/Berreman/<name>.gates` descriptor first") this new test suite REQUIRES
  such a descriptor, yet slice 001's files-in-scope creates only the test
  *project* (`OpticalConstructor.Tests.fsproj`), not the gate definition, and no
  later slice creates it either. With the gate id unresolvable, the gate roster
  cannot be loaded/run for slice 001 — and therefore for every slice in the arc.
  (Rubric §g: "Every gate id resolves … Missing -> BLOCKER.") This is the one
  finding that blocks the arc from its first round; it reads as a gates-cycle /
  infra gap rather than a slicing-logic defect, but it must be closed before any
  worker round can pass.

Other manifest-header checks pass: required keys present (`repo_root`,
`branch`, `worker_timeout_sec`=14400, `worker_hangup_idle_sec`=3600,
`worker_review_max_cycles`=3, `max_total_route_backs`=30); timeouts sane
(600 ≤ 14400 ≤ 43200; 3600 ≥ 300 and < 14400); `repo_root` `C:/GitHub/Berreman`
and the two `user_files` role prompts resolve on disk; `.artifacts/` exists;
the manifest body slice list 001–016 matches the slice files on disk. No
slice-spec routes persistent output outside `<spec>/.artifacts/` (`.binz`
sidecars are correctly confined to the project working folder per §I.4/§F.2,
not `%TEMP%`/`%LOCALAPPDATA%`).

## h. Per-slice quality

_no findings_

Every slice carries Scope, numbered Requirements (R-N traced to spec §§),
Non-requirements, Files modified (planned), Testing plan, Risks, and Hand-off
(plus the inherited binding-constraints block and owned ACs / file:line refs).
No slice is missing a required section. Each hand-off correctly reminds the
worker to emit the state-of-the-world file with all required ATX headings and a
`gates:` YAML baseline block for the next slice's `count_at_least` gate.

## i. Other

- **MINOR** -- Part F's behavioural test ownership is thin and deferred. Slice
  008 implements five analysis functions (`absorptance`, `stokesSystem`,
  `degreeOfPolarization`, `psiDelta`/`ncs`, `fieldDepthProfile`,
  `layerAbsorptance`/`totalAbsorbedPower`, colorimetry) plus the engine edit,
  but mandates only the `MuellerMatrixTests.fs` guard in-slice; the primary
  AC-F3/F5/F7/F8 assertions are routed three slices later into slice 011's
  `BerremanTests/OptimizationTests.fs`, and AC-F1/F2/F4/F6/F9 have no clearly
  *owned* mandatory test (slice 008 marks F1/F4 "MAY add if convenient" and
  says F2/F6/F9 are "exercised where slice 011/Part H wires"). The routing is an
  explicit reconciler decision and the code earns compile coverage immediately,
  so this is not blocking — but it leaves several Part F ACs without an
  unambiguous test home and weakens the test-first discipline for that slice.
  Consider giving F1/F4/F6/F9 a small owned smoke assertion in slice 008's own
  `BerremanTests`/constructor tests rather than leaning entirely on the slice-011
  bundle.
- (Non-finding, noted) Slice 006's hand-off prose calls Part H "optimizers"
  (Part H is Charts); harmless doc nit, not worth a fix.

---

## Recommendations (priority order)

1. **Close the `constructor-unit-tests` gate (BLOCKER, §g).** Author
   `.gates/Berreman/constructor-unit-tests.gates` (a `kind: command`
   `dotnet test --no-build -c Release` descriptor with `cwd:
   OpticalConstructor/OpticalConstructor.Tests`, `pass_when.count_at_least` on a
   new `baseline_key`, mirroring `unit-tests.gates`) and add its creation to
   slice 001's files-in-scope/Requirements (R-6) — or, if the gates folder is
   operator/gates-cycle-owned, have the operator land the descriptor before the
   arc starts. Until the gate id resolves, no slice can pass its roster.
2. **Give Part F ACs an owned in-slice test home (MINOR, §i).** Add small
   mandatory smoke assertions for AC-F1/F4/F6/F9 (and the AC-F2 `.binz`-sidecar
   persistence) in slice 008 itself rather than deferring all behavioural
   coverage to slice 011's `OptimizationTests.fs`.
3. **Keep slice 002 under review during execution (MINOR, §c).** It is the
   heaviest, most load-bearing slice; if the worker shows signs of overrun, it
   is the natural candidate to split the units spine (D.1–D.4) from the
   beam-tree topology (A.4/B.2–B.4/B.8/B.11). No pre-emptive split required.
