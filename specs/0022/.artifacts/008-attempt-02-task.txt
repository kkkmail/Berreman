# Arc-runner worker task -- 008.slice-md

## Concrete paths for this round

The supervisor pre-resolves every per-slice path you need this
round and lists them below. Do not compute or guess these
paths: the canonical layout block describes the shape, this
section pins the concrete values for the slice you are about
to implement.

- **spec_folder:** `C:\GitHub\Berreman\specs\0022`
- **slice_md_path:** `C:\GitHub\Berreman\specs\0022\.slices\008.slice-md`
- **artifacts_folder:** `C:\GitHub\Berreman\specs\0022\.artifacts`
- **impl_log_path:** `C:\GitHub\Berreman\specs\0022\.slices\008-impl-log.md`
- **impl_plan_path:** `C:\GitHub\Berreman\specs\0022\.slices\008-impl-plan.md`
- **state_of_world_path:** `C:\GitHub\Berreman\specs\0022\.slices\008-state-of-the-world.md`
- **spec_id:** `0022`
- **slice_id:** `008`


## System prompt

Read the cross-repo system prompt in full before anything else:

- C:\GitHub\AI-Strategy-Generator\src\ai_strategy_generator\multistep\arc-runner.system-md

## Project prompt

Read the per-project prompt in full:

- C:\GitHub\Berreman\.user-md\Berreman\arc-runner.user-md

## Slice spec

Implement this slice end-to-end:

- C:\GitHub\Berreman\specs\0022\.slices\008.slice-md

## Outputs you MUST write (and nowhere else)

**Save your state-of-the-world markdown to**
`C:\GitHub\Berreman\specs\0022\.slices\008-state-of-the-world.md`. **Do not write the SoW anywhere else.**

**Save your impl-log markdown to**
`C:\GitHub\Berreman\specs\0022\.slices\008-impl-log.md`. **Do not write the impl-log anywhere else.**

## Artifacts folder

Use `C:\GitHub\Berreman\specs\0022\.artifacts` for any persistent run artifacts
(captured logs, screenshots, traces, dumps). The folder is
pre-created; this path is the arc-runner's authoritative
value and supersedes any path the project prompt computes
from convention.

## Retry hint

Strip the incidental trailing-whitespace/EOL normalization from FieldFunctions.fs and both .fsproj files so the diff collapses to only the real edits: the `member em.muellerMatrix` completion at FieldFunctions.fs:93 and the <Compile>/<ProjectReference>/<PackageReference> adds. Re-apply only those lines; revert every EOL-only change (verify with `git diff HEAD --ignore-space-at-eol`). Do NOT modify any working analysis code or test logic — all three gates are green and must stay green. Optional, same cleanup: in MuellerMatrixTests reuse `MatrixComparison.allowedDiff` (via `open BerremanTests.MatrixComparison`) instead of the fresh `1.0e-10` literal, per the project prompt's tolerance convention.

