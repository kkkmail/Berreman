# Arc-runner worker task -- 004.slice-md

## Concrete paths for this round

The supervisor pre-resolves every per-slice path you need this
round and lists them below. Do not compute or guess these
paths: the canonical layout block describes the shape, this
section pins the concrete values for the slice you are about
to implement.

- **spec_folder:** `C:\GitHub\Berreman\specs\0022`
- **slice_md_path:** `C:\GitHub\Berreman\specs\0022\.slices\004.slice-md`
- **artifacts_folder:** `C:\GitHub\Berreman\specs\0022\.artifacts`
- **impl_log_path:** `C:\GitHub\Berreman\specs\0022\.slices\004-impl-log.md`
- **impl_plan_path:** `C:\GitHub\Berreman\specs\0022\.slices\004-impl-plan.md`
- **state_of_world_path:** `C:\GitHub\Berreman\specs\0022\.slices\004-state-of-the-world.md`
- **spec_id:** `0022`
- **slice_id:** `004`


## System prompt

Read the cross-repo system prompt in full before anything else:

- C:\GitHub\AI-Strategy-Generator\src\ai_strategy_generator\multistep\arc-runner.system-md

## Project prompt

Read the per-project prompt in full:

- C:\GitHub\Berreman\.user-md\Berreman\arc-runner.user-md

## Slice spec

Implement this slice end-to-end:

- C:\GitHub\Berreman\specs\0022\.slices\004.slice-md

## Outputs you MUST write (and nowhere else)

**Save your state-of-the-world markdown to**
`C:\GitHub\Berreman\specs\0022\.slices\004-state-of-the-world.md`. **Do not write the SoW anywhere else.**

**Save your impl-log markdown to**
`C:\GitHub\Berreman\specs\0022\.slices\004-impl-log.md`. **Do not write the impl-log anywhere else.**

## Artifacts folder

Use `C:\GitHub\Berreman\specs\0022\.artifacts` for any persistent run artifacts
(captured logs, screenshots, traces, dumps). The folder is
pre-created; this path is the arc-runner's authoritative
value and supersedes any path the project prompt computes
from convention.

## Retry hint

Fix the duplicate "description" key in the `dispersionModel` $def of `OpticalConstructor.Storage/schema/optical-constructor-project.schema.json`: there are two `"description"` members (one after `"type":"object"`, one after the `properties` block) and JSON last-wins drops the meaningful model description — fold the two into a single key so the authored text survives a load. While in the diff, fold reuse-critic F1: lift one `isotropicProperties` helper into `DispersionModels.fs` (Domain) and call it from `toOpticalProperties`/`toAnisotropicOpticalProperties` and from `MaterialImport.fs` instead of the three inline `Mu.vacuum.dispersive`/`Rho.vacuum.dispersive` literals. Do NOT attempt to fix the schema↔serializer tag-shape mismatch or the closure-vs-DispersionModel round-trip gap — those are deferred to the materials-wiring slice; just carry them forward as an explicit hand-off risk in the SoW. Keep all gates green.

