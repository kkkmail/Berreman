# Reuse critique -- .spec-md cycle 1

## Coverage

- Helper roots walked: `C:\GitHub\Berreman` (repo root).
- Files inspected: diff scope is 3 non-code files; walk extensions matched 408 `.md` / 14 `.json` / 0 `.py`, all under the 200/root cap (no cap-trip). Configured extensions `.py,.md,.json` catch none of the F# solver source, and the diff touches none of it.
- Extensions: `.py`, `.md`, `.json`.
- Diff against `HEAD` (`git diff HEAD`): `specs/0035/.spec-md` (steering narrative), `specs/0035/.history`, `specs/0035/.artifacts/cost.json`. No `.fs`/`.fsproj`/test files changed.
- Note: this file supersedes a prior critique of the now-committed `.contract-ids/XDUO-json` registration; that change is part of `HEAD` and is no longer in the worker diff.

## Findings

No reuse findings; the diff introduces no duplication of existing helpers within the walked roots.

Rationale (not a finding — recorded so the judge sees the read): this cycle's diff is a **spec-writing** change, not an implementation slice. The task file itself confirms the posture — "(Stub template: full content lands in slice 004-08.)" The only substantive edit is to the steering narrative `specs/0035/.spec-md`: four claim-check reconciliations applied by the spec author — a pointer bump on the `mainView` reference (`…TestWindows/TableAndElementRotationView.fs:2281`→`:2298`), and three un-anchored assertions re-anchored to concrete pointers (`…Domain/MaterialComplexityEditor.fs:697` for the lossless-uncheck mechanic; `…OpticalConstructor.Tests/SampleProxyTests.fs:120` for the empty-sample negative case; `…Domain/MaterialComplexityEditor.fs:699` for the derived-model invariant). The remaining two files are arc bookkeeping — a `.history` append and a `cost.json` ledger entry.

The reuse critic evaluates whether a worker's diff reinvents functions, helper modules, fixtures, or test scaffolding that already exist. This diff adds none of those — no F# symbol, no fixture, no test helper, no new module. There is no code artifact to compare against the existing helper catalogue, and the rubric is explicit that a finding without a citable existing helper is noise, not a finding. So there is no direct duplication, no near-miss duplication, and no pattern divergence to report for this cycle.

Where the diff *could* have diverged, it does the opposite — it strengthens reuse. Every edit points the future implementer at an existing seam rather than sketching a new one. This is worth surfacing for the judge (as the inverse of a finding, not a finding): the spec being steered is markedly reuse-conscious, and the reconciliations sharpened that. `<STORE:CategoryProxy>` is specified to mirror `SampleProxy.createInMemory` / `MaterialProxy.createInMemory` and the `LibraryProxy` shape (`…Domain/ElementId.fs:616,686,276`, `.spec-md` §0.3); its `removeCategory` hard-block is specified to mirror `MaterialProxy.removeMaterial`'s `MaterialStillReferenced` (`…Domain/MaterialLibrary.fs:135`; assembled `ElementId.fs:715`) with a `materialsReferencingCategory` seam paralleling `samplesReferencing` (`…Domain/ElementId.fs:740`); `<UICOMP:CategoryControls>` is specified to follow the `LibraryControls` shape (§A.2). The dispersive ρ/μ rungs (§C) are specified to route through the same `DispersionModels.modelParameters` + raw `SumOfTerms` surface built in 0033 (`…Domain/DispersionModels.fs:291`) and to reuse the `toComplexity` lossless-uncheck mechanic (`…Domain/MaterialComplexityEditor.fs:697`). The n/k preview (§E) is specified to reuse the already-real-moved shared ScottPlot control (`…Controls/ExperimentChart.fs`, `…Controls/ChartWindow.fs`) and the unchanged pure builder `NkDispersionChart.nkDispersionChart` (`:55`) while *deleting* the hand-rolled `NkDispersionChart.inlineCanvas` (`:108`) — retiring a duplicate rather than adding one. These are reuse *mandates*, not duplications.

The substantive reuse questions the spec raises — will `CategoryProxy.createInMemory` actually reuse the `MaterialProxy`/`SampleProxy` in-memory closure pattern instead of hand-rolling a parallel one; will `CategoryControls` reuse `LibraryControls` rather than clone it; will the dispersive-ρ/μ editor reuse `DispersionModels.modelParameters` + the `SumOfTerms` surface rather than re-implement a coefficient editor; will the preview genuinely embed `ExperimentChart` rather than keep `inlineCanvas` — are all deferred to the implementation slices (004-08 onward) where real F# code lands. They cannot be assessed against a narrative-only diff, and inventing findings against code that does not yet exist would violate the rubric's "reuse what exists, do not design what doesn't" constraint. (Whether each cited helper actually exists at its cited line is a claim-check / architecture-critic concern; the `.history` shows a claim-check pass already ran and was reconciled — that is the source of this diff — so it is outside this reuse-critic's remit.)

## Bottom line

The diff is a spec-narrative reconciliation plus arc bookkeeping with zero F# helpers, fixtures, or test scaffolding, so nothing in it could duplicate an existing helper — and the edits in fact tighten the spec's existing reuse mandates rather than loosen them. My read is that this cycle is clean and gives the judge no reuse basis to re-spawn. The real reuse surface (proxy `createInMemory` reuse, `LibraryControls` reuse, `DispersionModels`/`ExperimentChart` reuse) arrives with the implementation slices and should be critiqued then.
